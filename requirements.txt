# <0.130: starting with 0.130, include_router wraps routes in _IncludedRouter
# and breaks the APIRoute introspection used by tests and tooling.
fastapi<0.130
uvicorn[standard]
python-multipart
# Native text-block boundaries changed between recent releases and directly
# affect deterministic EvidenceUnit/window inventories.  Pin the runtime used
# by the v9 campaign so replay digests are reproducible.
pymupdf==1.27.1
openai>=2.50.0,<3
pydantic-settings
pyyaml
langgraph
jsonschema
networkx
json-repair
openpyxl>=3.1,<4
